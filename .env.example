# TMDb Auth (recommended)
# Create a TMDb v4 Read Access Token and set it here.
TMDB_ACCESS_TOKEN=

# Optional fallback (less preferred):
TMDB_API_KEY=

# Optional: set default region for watch providers (ISO 3166-1)
DEFAULT_WATCH_REGION=US
